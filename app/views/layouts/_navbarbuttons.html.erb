<div class="navbar-form navbar-right">
  <% if current_user %>
   <%= link_to "Logout", session_path("current"), method: :delete, class: "btn btn-submit" %></button>
  <% else %>
    <%= link_to "Login", new_session_path, class: "btn btn-submit" %>
    <%= link_to "Sign Up", new_user_path, class: "btn btn-submit" %>
  <% end %>    
</div>